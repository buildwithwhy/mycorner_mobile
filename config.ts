// Configuration file for API keys and settings

// To use Google Places Autocomplete and geocoding:
// 1. Get a Google Maps Platform API key from: https://console.cloud.google.com/
// 2. Enable these APIs: Places API, Geocoding API, Maps SDK
// 3. Replace 'YOUR_GOOGLE_MAPS_API_KEY' below with your actual key
// 4. For production, use environment variables instead of hardcoding

export const GOOGLE_MAPS_API_KEY = 'YOUR_GOOGLE_MAPS_API_KEY';

// For development/testing, you can use a demo key, but it will have limited requests
// For production, always use a proper API key with billing enabled and restrictions set
